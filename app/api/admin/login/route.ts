import{NextResponse}from"next/server";import{signAdmin}from"../../../../lib/admin-session";export async function POST(req:Request){const b=await req.json().catch(()=>null);let a:any;try{a=(JSON.parse(process.env.ADMIN_CREDENTIALS||"[]") as any[]).find(x=>x.id===String(b?.adminId||"").trim()&&x.pin===String(b?.adminPin||""))}catch{}if(!a)return NextResponse.json({error:"Invalid Admin ID or PIN."},{status:401});const admin={name:a.name||a.id};const r=NextResponse.json({admin});r.cookies.set("hostelresolve_admin",signAdmin(admin),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:43200});return r}